 # AI-video 项目指令
 
 ## Git 工作流
 
 每次完成一个功能模块或逻辑单元后，自动执行 git 提交：
 
 1. 运行 `git add -A`
 2. 运行 `git commit -m "feat: <模块名>: <改动摘要>"`
 
 什么是"功能模块"：一个独立完整的改动，比如：
 - 一个新的 API 端点及其 Service
 - 一个新的页面及其组件
 - 一个新的数据库模型或 Prisma migration
 - 一个完整的 UI 功能
 - 一次有意义的重构
 
 原则：不要每改一个文件就 commit，也不要让多个不相关的功能堆在一个 commit 里。如果这个改动可以单独回退而不影响其他功能，它就值得单独 commit。
 如果有嵌套的子模块 git 目录（如 `apps/ai-video-api/.git`），请先删除它们，统一使用根目录的 git 仓库。
 
 ## 代码审查（自动触发）
 
 每次开发完一个功能模块并提交后，**自动执行代码审查**，无需用户额外请求。
 
 ### 审查流程
 
 1. **读取本次提交的文件**：`git diff HEAD~1 --name-only`
 2. **逐文件审查**：阅读每个新增/修改的代码文件
 3. **生成审查报告**，包含：
    - 整体评价（优点 + 问题）
    - 问题列表，按严重度分级：
      - 🔴 P0（安全/权限）：如接口缺少权限校验、SQL注入风险、敏感信息泄露
      - 🟡 P1（逻辑/体验）：如边界情况未处理、状态管理缺陷、UX 问题
      - 🟢 P2（代码质量）：如未使用变量、冗余代码、可读性差、缺少注释
    - 每个问题包含：位置（文件:行号）、描述、建议修复方式
    - 修复优先级建议
 4. **询问用户**：列出问题后询问"是否需要立即修复？"
 
 ### 审查重点
 
 - **安全性**：接口权限校验（@CurrentUser + verifyProjectAccess）、输入验证、敏感数据处理
 - **类型安全**：避免 `as any`、确保类型定义完整、前后端类型对齐
 - **边界情况**：空值处理、错误处理、loading/error 状态
 - **代码质量**：单一职责、可复用性、命名规范、未使用代码
 - **前端体验**：自动保存、错误提示、loading 状态、键盘操作
 - **测试覆盖**：新增功能是否有对应测试用例
